{
	"name": "bonjourr-apis",
	"author": "Victor Azevedo",
	"description": "This regroups all APIs used by Bonjourr as a single cloudflare worker",
	"license": "ISC",
	"type": "module",
	"repository": {
		"type": "git",
		"url": "https://github.com/victrme/bonjourr-apis"
	},
	"scripts": {
		"test": "vitest",
		"dev": "wrangler dev --env update",
		"init": "git submodule update --init --recursive",
		"sync": "git submodule update --remote --recursive && git add ./src/apis/** && git commit -m \"Updated submodules\""
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20240405.0",
		"vitest": "^1.4.0",
		"wrangler": "^3.48.0"
	}
}
